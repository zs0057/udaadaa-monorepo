# Udaadaa Migration Agent Instructions

## 프로젝트 목적

기존 Flutter·Supabase 기반 Udaadaa를 분석하고, 서비스 중단과 데이터 유실을 방지하면서 Spring 기반 백엔드로 점진적으로 마이그레이션한다.

## 저장소 구조

- `udaadaa/`: 기존 Flutter 애플리케이션과 Supabase 구성. 현재 동작을 확인하는 기준이다.
- `udaadaa_server/`: 새 Spring 백엔드 구현.
- `docs/migration/`: 시스템 분석, 아키텍처 및 마이그레이션 문서.
- `docs/ai/`: Codex와 AI 에이전트의 작업 방식.

## 필수 작업 원칙

- 기존 구현을 확인한 사실과 추측을 명확히 구분한다.
- 기존 `udaadaa/` 코드는 명시적인 변경 요청이 없으면 수정하지 않는다.
- 기능을 도메인 단위로 점진적으로 이전하고 한 번에 전체 시스템을 교체하지 않는다.
- 같은 기능의 데이터 쓰기 주체를 Flutter와 Spring에 동시에 두지 않는다.
- 아키텍처와 도메인 경계는 AI가 제안할 수 있지만 최종 결정은 사용자와 합의한다.
- 비밀 키, 토큰, 개인정보를 문서·로그·프롬프트에 복사하지 않는다.
- 구현을 변경한 경우 관련 테스트와 정적 검사를 실행하고, 실행하지 못한 검증은 결과에 명시한다.
- 구현과 문서가 달라지면 관련 문서를 함께 갱신한다.

## AI 작업 방식

- 복잡한 작업은 `docs/ai/task-technique-guide.md`에서 적합한 기법과 검증 방식을 확인한다.
- 독립적인 병렬 조사가 실질적인 이점이 있을 때만 `docs/ai/subagent-policy.md`에 따라 서브에이전트를 사용한다.
- 메인 에이전트는 요구사항, 의사결정, 결과 통합과 최종 검증을 책임진다.
- 반복되는 실수가 확인될 때만 이 파일에 새로운 규칙을 추가한다.

## 완료 기준

- 요청한 산출물이 구현되거나 문서화되었다.
- 관련 테스트 또는 검증 결과가 확인되었다.
- 변경 범위, 남은 위험과 후속 작업이 사용자에게 명확히 전달되었다.
